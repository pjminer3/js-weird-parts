"use strict"
// assigning a variable that hasn't been defined throws an error
// can only put 'use strict' at the top of a file or the top of a function (to set a function to strict mode)


var person;

persom = {};
console.log(persom);